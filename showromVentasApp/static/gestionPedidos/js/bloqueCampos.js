// Función para bloquear los campos
/* function bloquearCampos() {
    // Ejemplo para bloquear el input del cliente
    const clienteInput = document.getElementById("inputCliente");
    if (clienteInput) {
        clienteInput.disabled = true;
    }

    // Bloquea otros campos que quieras
    const productInputs = document.querySelectorAll('.product-row input');
    productInputs.forEach(input => {
        input.disabled = true;
    });
} */

